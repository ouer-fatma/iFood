{% extends 'base.html.twig' %}

{% block title %}Delivery{% endblock %}

{% block body %}
    <h1>Delivery</h1>
    <table class="table">
        <tbody>
        
               
            
                <th>Lang</th>
                <td  id="lang">{{ delivery.lang }}</td>
            
        
                <th>Attitude</th>
                <td id="lat">{{ delivery.attitude }}</td>
            
        
                <th>Price</th>
                <td>{{ delivery.price }} D.T</td>
            
        </tbody>
    </table>

    <div class="container-wrap">
        <div class="row no-gutters d-md-flex align-items-center">
            <div class="col-md-6 d-flex align-selftch">
                <div id="map"></div>
            </div>
          
        </div>
    </div>
    <a href="{{ path('app_delivery_index') }}">back to list</a>

    <a href="{{ path('app_delivery_edit', {'id': delivery.id}) }}">edit</a>

    {{ include('delivery/_delete_form.html.twig') }}
{% endblock %}
